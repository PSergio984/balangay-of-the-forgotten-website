---
phase: 01-the-wow-landing-page
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - components/ui/AudioHook.tsx
  - components/landing/ParallaxHero.tsx
  - app/layout.tsx
autonomous: false
requirements: [CORE-01, INT-03]
user_setup:
  - service: Audio Assets
    why: "Background atmospheric loops"
    dashboard_config:
      - task: "Place audio files in public/audio/"
        location: "Project Root"
must_haves:
  truths:
    - "Ambient music can be toggled via an unmute button"
    - "Parallax is disabled on mobile for performance (discretionary check)"
    - "Pixel crispness is maintained on high-DPI displays"
  artifacts:
    - path: "components/ui/AudioHook.tsx"
      provides: "Thematic audio management"
---

# Phase 1: Experience & Polish Implementation Plan

**Goal:** Finalize the immersive experience with an atmospheric audio hook and performance optimizations for mobile.

**Architecture:** Use `gsap.matchMedia()` to conditionally disable parallax on small screens. Implement a persistent audio controller using the HTML5 Audio API with a user-triggered start (per browser policy).

---

<objective>
Polish the "WOW" factor and ensure broad device compatibility.

Purpose: Add the final atmospheric layer (D-12) and ensure a smooth mobile experience (D-13).
Output: AudioHook component and responsive GSAP configurations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-the-wow-landing-page/01-03-SUMMARY.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
@.planning/phases/01-the-wow-landing-page/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ambient Music Hook</name>
  <files>components/ui/AudioHook.tsx, app/layout.tsx</files>
  <action>
- [ ] **Step 1: Create Audio Controller**
Implement a simple audio hook with a thematic "unmute" toggle.

```tsx
"use client";

import { useState, useRef } from "react";

export default function AudioHook() {
  const [isPlaying, setIsPlaying] = useState(false);
  const audioRef = useRef<HTMLAudioElement>(null);

  const toggle = () => {
    if (!audioRef.current) return;
    if (isPlaying) {
      audioRef.current.pause();
    } else {
      audioRef.current.play();
    }
    setIsPlaying(!isPlaying);
  };

  return (
    <div className="fixed bottom-32 right-32 z-50">
      <audio ref={audioRef} loop src="/audio/ambient-loop.mp3" />
      <button 
        onClick={toggle}
        className="bg-deep-sky border-4 border-midnight-blue p-8 font-pixel text-12 text-midnight-blue hover:bg-retro-gold transition-colors"
      >
        {isPlaying ? "MUTE AUDIO" : "UNMUTE AUDIO"}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Add to Layout**
Include the `AudioHook` in the `RootLayout`.

- [ ] **Step 3: Commit**
`git add . && git commit -m "feat(audio): add ambient music hook with user toggle"`
  </action>
  <verify>
    <automated>grep -r "AudioHook" app/layout.tsx</automated>
  </verify>
  <done>Audio toggle is visible and functional (pending audio file placement).</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Optimization & Responsive Audit</name>
  <files>components/landing/ParallaxHero.tsx</files>
  <action>
- [ ] **Step 1: Implement MatchMedia**
Update `ParallaxHero` to disable parallax on viewports smaller than 768px.

```tsx
  useGSAP(() => {
    let mm = gsap.matchMedia();

    mm.add("(min-width: 768px)", () => {
      // Parallax logic
      const tl = gsap.timeline({
        scrollTrigger: {
          trigger: container.current,
          start: "top top",
          end: "bottom top",
          scrub: true,
        }
      });

      tl.to(bgRef.current, { yPercent: -25, ease: "none" }, 0);
      tl.to(midRef.current, { yPercent: -15, ease: "none" }, 0);
      tl.to(fgRef.current, { yPercent: -5, ease: "none" }, 0);
    });

    mm.add("(max-width: 767px)", () => {
      // Static or simple fade for mobile
      gsap.set([bgRef.current, midRef.current, fgRef.current], { yPercent: 0 });
    });
  }, { scope: container });
```

- [ ] **Step 2: Commit**
`git add components/landing/ParallaxHero.tsx && git commit -m "perf(hero): disable parallax on mobile using gsap.matchMedia"`
  </action>
  <verify>
    <automated>grep -r "gsap.matchMedia" components/landing/ParallaxHero.tsx</automated>
  </verify>
  <done>Responsive performance guardrails are in place.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Final Acceptance Check</name>
  <action>
- [ ] **Step 1: Visual & Performance Review**
- Open the landing page in desktop and mobile (simulated) views.
- Confirm parallax is smooth on desktop.
- Confirm news dispatches stagger in correctly.
- Test the audio toggle.
- Verify pixel-perfect rendering (no blur on sprites/text).
  </action>
  <verify>Manual visual inspection</verify>
  <done>Phase 1 reaches Definition of Done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new boundaries.

## STRIDE Threat Register
No new threats.
</threat_model>

<verification>
- Verify audio plays only after user interaction.
- Verify parallax logic does not execute on mobile viewports.
</verification>

<success_criteria>
- Mobile experience is jitter-free.
- Atmospheric audio is optional and user-controlled.
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-04-SUMMARY.md` when done
</output>
