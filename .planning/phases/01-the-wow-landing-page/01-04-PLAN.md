---
phase: 01-the-wow-landing-page
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: ["components/landing/BulletinBoard.tsx", "components/audio/AmbientPlayer.tsx", "app/page.tsx", "scripts/audit-performance.sh"]
autonomous: true
requirements: [CORE-03, INT-03]
must_haves:
  truths:
    - "News feed items stagger into view like pinned notes (D-07)"
    - "Ambient music player is present with a clear unmute button (D-12)"
    - "Landing page meets performance and mobile pixel-crispness goals (D-13, SC-04)"
  artifacts:
    - path: "components/landing/BulletinBoard.tsx"
      provides: "Staggered thematic news board"
    - path: "components/audio/AmbientPlayer.tsx"
      provides: "Atmospheric audio loop with toggle"
---

<objective>
Finalize the landing page experience with the thematic news feed, ambient audio, and performance/mobile audits.

Purpose: Complete the "WOW" factor and ensure the project meets the high-quality bar for the first milestone.
Output: Polished, interactive landing page with audio and optimized performance.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-the-wow-landing-page/01-03-SUMMARY.md
@.planning/phases/01-the-wow-landing-page/01-CONTEXT.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BulletinBoard (News Feed)</name>
  <files>components/landing/BulletinBoard.tsx, app/page.tsx</files>
  <behavior>
    - `BulletinBoard` must render news items as "pinned notes" (cards with pixel borders).
    - News items must stagger in with a `0.1s` delay using GSAP.
    - Each note must show a date and brief summary.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/bulletin-board.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('BulletinBoard exists and uses staggered animations', () => {
      const component = fs.readFileSync('components/landing/BulletinBoard.tsx', 'utf-8');
      expect(component).toContain('stagger');
      expect(component).toContain('data-type="note"');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/bulletin-board.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/BulletinBoard.tsx`:
       - Use a grid layout for "notes".
       - Apply GSAP `stagger: 0.1` to the children in a `useGSAP` hook.
       - Use `PixelCard` primitives from the design system for individual notes.
    2. Add `BulletinBoard` section to the end of `app/page.tsx`.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/bulletin-board.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-04): implement staggered BulletinBoard news feed (D-07)"`
  </action>
  <verify>
    <automated>npx vitest tests/bulletin-board.test.ts</automated>
  </verify>
  <done>News bulletin board is functional and animated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ambient Music & Performance Audit</name>
  <files>components/audio/AmbientPlayer.tsx, app/layout.tsx, scripts/audit-performance.sh</files>
  <behavior>
    - `AmbientPlayer` must have a button to toggle (Unmute/Mute) the audio loop.
    - Audio should not autoplay without user interaction (browser policy).
    - Performance audit script must check for Lighthouse scores and mobile pixel crispness utilities.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/polish.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('AmbientPlayer exists with unmute toggle', () => {
      const component = fs.readFileSync('components/audio/AmbientPlayer.tsx', 'utf-8');
      expect(component).toContain('UNMUTE');
      expect(component).toContain('<audio');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/polish.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/audio/AmbientPlayer.tsx`:
       - Include an `<audio>` tag with `loop`.
       - Implement a simple stateful button for `playing` status.
       - Position fixed (e.g., bottom-right).
    2. Create `scripts/audit-performance.sh` to run `lighthouse-ci` or a simple `curl` check for critical headers and JS bundle sizes.
    3. Ensure `integer-ratio scaling` (D-13) is present in `globals.css` utilities.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/polish.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-04): add ambient audio and performance guardrails (D-12, D-13)"`
  </action>
  <verify>
    <automated>npx vitest tests/polish.test.ts</automated>
  </verify>
  <done>Experience is polished with audio and audited for performance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser Policy -> Audio | Interaction required before audio can play |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Metadata | mitigate | Strip EXIF data from game assets before commit |
</threat_model>

<verification>
Verify audio toggle functionality and news board staggering.
</verification>

<success_criteria>
- AmbientPlayer allows unmuting an audio loop.
- BulletinBoard items animate in a staggered sequence.
- Performance audit script passes (Lighthouse > 90).
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-04-SUMMARY.md` when done
</output>
