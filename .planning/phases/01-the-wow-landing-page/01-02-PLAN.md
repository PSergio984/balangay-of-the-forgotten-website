---
phase: 01-the-wow-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["components/landing/ParallaxHero.tsx", "components/landing/PixelNavbar.tsx", "app/page.tsx"]
autonomous: true
requirements: [CORE-02, CORE-01]
must_haves:
  truths:
    - "Hero section has multi-layer parallax scrolling (D-02, D-06)"
    - "Sticky pixelated navigation bar is present and functional (D-09)"
    - "Hero section renders crisp pixel art assets (SC-01)"
  artifacts:
    - path: "components/landing/ParallaxHero.tsx"
      provides: "Multi-layer GSAP parallax hero"
    - path: "components/landing/PixelNavbar.tsx"
      provides: "Sticky pixel-art navigation"
---

<objective>
Implement the core "WOW" factor: the multi-layer parallax hero section and the thematic sticky navigation bar.

Purpose: Deliver the immediate visual impact and brand identity defined in UI-SPEC.
Output: Functional parallax hero and navigation system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-the-wow-landing-page/01-01-SUMMARY.md
@.planning/phases/01-the-wow-landing-page/01-CONTEXT.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParallaxHero Component</name>
  <files>components/landing/ParallaxHero.tsx, app/page.tsx</files>
  <behavior>
    - `ParallaxHero` must render at least three layers (background, mid, foreground).
    - GSAP `ScrollTrigger` must be used to move layers at different rates.
    - Assets must use `image-rendering: pixelated`.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/parallax-hero.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('ParallaxHero component exists and uses GSAP', () => {
      const component = fs.readFileSync('components/landing/ParallaxHero.tsx', 'utf-8');
      expect(component).toContain('useGSAP');
      expect(component).toContain('ScrollTrigger');
      expect(component).toContain('data-layer="background"');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/parallax-hero.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/ParallaxHero.tsx`:
       - Use `useGSAP` hook from `@/lib/gsap`.
       - Define three layers with `data-layer` attributes.
       - Apply GSAP animations: background (`yPercent: -25`), mid (`yPercent: -15`), foreground (`yPercent: -5`).
       - Use Next.js `Image` with `priority` and `pixelated` rendering.
    2. Import and add `ParallaxHero` to `app/page.tsx`.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/parallax-hero.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-02): implement multi-layer parallax hero (D-02, D-06)"`
  </action>
  <verify>
    <automated>npx vitest tests/parallax-hero.test.ts</automated>
  </verify>
  <done>Parallax hero is implemented with GSAP scrubbing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sticky Pixel Navbar</name>
  <files>components/landing/PixelNavbar.tsx, app/layout.tsx</files>
  <behavior>
    - `PixelNavbar` must be sticky at the top of the viewport.
    - Must use pixel-art borders and "Press Start 2P" font for links.
    - Navigation must include "Explore", "Logbook", and "Wiki" links.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/navbar.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('Sticky Pixel Navbar exists with correct links', () => {
      const component = fs.readFileSync('components/landing/PixelNavbar.tsx', 'utf-8');
      expect(component).toContain('sticky');
      expect(component).toContain('EXPLORE');
      expect(component).toContain('LOGBOOK');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/navbar.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/PixelNavbar.tsx`:
       - Use `sticky top-0 z-40` classes.
       - Apply pixelated border styles using Tailwind v4 custom utilities.
       - Add NavLinks with "Press Start 2P" typography.
    2. Add `PixelNavbar` to `app/layout.tsx` above the `children`.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/navbar.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-02): implement sticky pixel navbar (D-09)"`
  </action>
  <verify>
    <automated>npx vitest tests/navbar.test.ts</automated>
  </verify>
  <done>Sticky navigation is functional and thematic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side JS -> GSAP | GSAP animations run in the browser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | GSAP ticker | mitigate | Ensure proper cleanup with `useGSAP` to prevent memory leaks |
</threat_model>

<verification>
Verify ParallaxHero layer movement and Navbar sticky behavior.
</verification>

<success_criteria>
- ParallaxHero renders 3+ layers with GSAP.
- Navbar is sticky and displays pixel-art styling.
- Scroll performance remains smooth (Lenis integration).
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-02-SUMMARY.md` when done
</output>
