---
phase: 01-the-wow-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".gitattributes", "package.json", "lib/gsap.ts", "app/globals.css", "app/layout.tsx"]
autonomous: true
requirements: [CORE-01, INT-03]
must_haves:
  truths:
    - "Project is initialized with Next.js 15 and Tailwind v4"
    - "Git LFS is configured to track game assets (D-01)"
    - "GSAP and Lenis are installed and configured (D-02, D-03)"
    - "Global CRT scanline overlay is present in the layout (INT-03)"
  artifacts:
    - path: ".gitattributes"
      provides: "Git LFS tracking for binaries"
    - path: "lib/gsap.ts"
      provides: "Centralized GSAP plugin registration"
    - path: "app/globals.css"
      provides: "Tailwind v4 theme with pixel units"
---

<objective>
Initialize the Balangay project foundation, including Git LFS for asset management, core motion dependencies (GSAP/Lenis), and the global pixel-art thematic layout.

Purpose: Ensure a stable, high-performance foundation for the "WOW" landing page.
Output: Configured Next.js project with thematic base styles.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-the-wow-landing-page/01-CONTEXT.md
@.planning/phases/01-the-wow-landing-page/RESEARCH.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project Init & Git LFS</name>
  <files>.gitattributes, package.json</files>
  <behavior>
    - Verify that the project repository is initialized.
    - Verify that Git LFS is installed and tracking common game asset extensions (.png, .avif, .mp3, .mp4).
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create a script `scripts/verify-init.sh`:
    ```bash
    #!/bin/bash
    if [ ! -f "package.json" ]; then echo "package.json missing"; exit 1; fi
    if ! git lfs ls-files > /dev/null 2>&1; then echo "Git LFS not initialized"; exit 1; fi
    if ! grep -q "*.png filter=lfs" .gitattributes; then echo "LFS not tracking PNGs"; exit 1; fi
    echo "Init verified"
    ```

    **Step 2: Run and verify failure**
    Run: `bash scripts/verify-init.sh`
    Expected: "package.json missing" (Exit code 1)

    **Step 3: Minimal Implementation**
    1. Initialize Next.js: `npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir=false --import-alias="@/*"` (Per RESEARCH.md)
    2. Initialize Git LFS:
       ```bash
       git lfs install
       git lfs track "*.png"
       git lfs track "*.avif"
       git lfs track "*.mp3"
       git lfs track "*.mp4"
       ```
    3. Ensure `.gitattributes` is added.

    **Step 4: Run and verify pass**
    Run: `bash scripts/verify-init.sh`
    Expected: "Init verified"

    **Step 5: Commit**
    Command: `git add . && git commit -m "chore(01-01): init project and git lfs (D-01)"`
  </action>
  <verify>
    <automated>bash scripts/verify-init.sh</automated>
  </verify>
  <done>Next.js project exists and Git LFS is tracking binary assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core Dependencies & GSAP Config</name>
  <files>package.json, lib/gsap.ts, components/providers/LenisProvider.tsx</files>
  <behavior>
    - GSAP, @gsap/react, and Lenis must be installed.
    - `lib/gsap.ts` must export ScrollTrigger and useGSAP.
    - `LenisProvider.tsx` must synchronize Lenis with the GSAP ticker.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/core-deps.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import { ScrollTrigger } from '../lib/gsap';

    test('GSAP and lib/gsap.ts exist', () => {
      expect(ScrollTrigger).toBeDefined();
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/core-deps.test.ts`
    Expected: Error (Module not found: ../lib/gsap)

    **Step 3: Minimal Implementation**
    1. Install deps: `npm install gsap @gsap/react lenis`
    2. Create `lib/gsap.ts` per RESEARCH.md:
       ```typescript
       "use client";
       import gsap from "gsap";
       import { ScrollTrigger } from "gsap/ScrollTrigger";
       import { useGSAP } from "@gsap/react";
       if (typeof window !== "undefined") {
         gsap.registerPlugin(ScrollTrigger, useGSAP);
       }
       export * from "gsap";
       export { ScrollTrigger, useGSAP };
       ```
    3. Create `components/providers/LenisProvider.tsx` per RESEARCH.md to sync with `gsap.ticker`.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/core-deps.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-01): install gsap/lenis and configure provider (D-02, D-03)"`
  </action>
  <verify>
    <automated>npx vitest tests/core-deps.test.ts</automated>
  </verify>
  <done>Motion libraries are installed and integrated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Global Theme & CRT Overlay</name>
  <files>app/globals.css, app/layout.tsx</files>
  <behavior>
    - `globals.css` must use Tailwind v4 and define pixel units.
    - `layout.tsx` must include a global CRT scanline overlay (pseudo-element or div).
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/theme.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('CRT scanline overlay exists in layout', () => {
      const layout = fs.readFileSync('app/layout.tsx', 'utf-8');
      expect(layout).toContain('crt-overlay');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/theme.test.ts`
    Expected: FAIL ("crt-overlay" not found)

    **Step 3: Minimal Implementation**
    1. Update `app/globals.css` with Tailwind v4 `@theme` block from UI-SPEC (pixel units, fonts).
    2. Add `.crt-overlay` CSS to `globals.css` (pointer-events-none, fixed, z-50, linear-gradient for scanlines).
    3. Wrap `children` in `app/layout.tsx` with a div containing `crt-overlay` class.
    4. Apply `image-rendering: pixelated` to global styles.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/theme.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-01): implement pixel-art theme and CRT overlay (D-04, INT-03)"`
  </action>
  <verify>
    <automated>npx vitest tests/theme.test.ts</automated>
  </verify>
  <done>Global thematic styles and CRT effect are active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Landing Page | External user interacts with the UI (Scroll/Click) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm installs | mitigate | Use `npm ci` in CI/CD; audit packages in RESEARCH.md |
| T-01-02 | Spoofing | Asset loading | mitigate | Use Next.js Image optimization and integrity checks if using CDNs |
</threat_model>

<verification>
Verify project initialization, dependency presence, and global CSS theme application.
</verification>

<success_criteria>
- package.json exists with Next.js 15.
- .gitattributes exists with LFS patterns.
- globals.css contains Tailwind v4 pixel definitions.
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-01-SUMMARY.md` when done
</output>
