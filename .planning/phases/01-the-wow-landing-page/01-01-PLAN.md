---
phase: 01-the-wow-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/globals.css
  - lib/gsap.ts
  - components/providers/LenisProvider.tsx
  - app/layout.tsx
  - components/ui/PixelNavbar.tsx
autonomous: true
requirements: [CORE-01, INT-03]
must_haves:
  truths:
    - "Application boots with Next.js 15 and Tailwind v4"
    - "Scrolling is smooth (Lenis) and synchronized with GSAP"
    - "UI has a global CRT scanline overlay and pixel-perfect rendering"
    - "Sticky Pixel Navbar is visible and functional"
  artifacts:
    - path: "app/globals.css"
      provides: "Pixel theme and Tailwind v4 config"
    - path: "lib/gsap.ts"
      provides: "GSAP registry"
    - path: "components/providers/LenisProvider.tsx"
      provides: "Smooth scroll wrapper"
    - path: "components/ui/PixelNavbar.tsx"
      provides: "Thematic navigation"
  key_links:
    - from: "app/layout.tsx"
      to: "components/providers/LenisProvider.tsx"
      via: "Component Wrapping"
    - from: "components/providers/LenisProvider.tsx"
      to: "lib/gsap.ts"
      via: "Ticker Synchronization"
---

# Phase 1: Foundation & Thematic Layout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize the Next.js 15 foundation with Tailwind v4, GSAP, and Lenis to establish the pixel-perfect identity and smooth motion infrastructure.

**Architecture:** Use a centralized GSAP registry for plugin management and a ReactLenis root provider synchronized via the GSAP ticker for frame-perfect scroll animations. The UI uses a global CRT scanline overlay and pixel-unit spacing.

**Tech Stack:** Next.js 15 (App Router), Tailwind CSS v4, GSAP (ScrollTrigger, @gsap/react), Lenis, shadcn/ui.

---

<objective>
Establish the project's technical foundation and core layout to enable high-end parallax effects and a thematic "in-universe" UI.

Purpose: Set up the stack required for the "WOW" factor while ensuring pixel-perfect rendering.
Output: Initialized repo, motion providers, and sticky navbar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-the-wow-landing-page/01-CONTEXT.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
@.planning/phases/01-the-wow-landing-page/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Theme Config</name>
  <files>package.json, app/globals.css, tailwind.config.ts</files>
  <action>
- [ ] **Step 1: Initialize Next.js 15 app**
Run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"`

- [ ] **Step 2: Install core animation dependencies**
Run: `npm install gsap @gsap/react lenis`

- [ ] **Step 3: Configure Tailwind v4 & Pixel Units**
Update `app/globals.css` to use Tailwind v4 directives and the theme registry from UI-SPEC.

```css
@import "tailwindcss";

@theme {
  --spacing: 1px;
  
  /* Color Palette (D-05) */
  --color-sky-mist: #F0F9FF;
  --color-deep-sky: #0EA5E9;
  --color-retro-gold: #F97316;
  --color-midnight-blue: #0C4A6E;

  /* Fonts (D-08) */
  --font-pixel: "Press Start 2P", system-ui;
  --font-serif-lore: "Crimson Text", serif;
}

@utility rendering-pixelated {
  image-rendering: pixelated;
  image-rendering: crisp-edges;
}

@utility crt-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: linear-gradient(
    rgba(18, 16, 16, 0) 50%, 
    rgba(0, 0, 0, 0.1) 50%
  ), linear-gradient(
    90deg, 
    rgba(255, 0, 0, 0.03), 
    rgba(0, 255, 0, 0.01), 
    rgba(0, 0, 255, 0.03)
  );
  background-size: 100% 4px, 3px 100%;
  pointer-events: none;
  z-index: 9999;
}

html, body {
  background-color: var(--color-sky-mist);
  color: var(--color-midnight-blue);
  overflow-x: hidden;
}

img, canvas {
  @apply rendering-pixelated;
}
```

- [ ] **Step 4: Commit**
`git add . && git commit -m "feat(foundation): init next.js 15 and tailwind v4 theme"`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next.js app initialized with Tailwind v4 pixel-unit configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Motion Registry & Scroll Provider</name>
  <files>lib/gsap.ts, components/providers/LenisProvider.tsx, app/layout.tsx</files>
  <action>
- [ ] **Step 1: Create GSAP Registry**
Create `lib/gsap.ts` to register plugins for React 19 compatibility.

```typescript
"use client";

import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { useGSAP } from "@gsap/react";

if (typeof window !== "undefined") {
  gsap.registerPlugin(ScrollTrigger, useGSAP);
}

export * from "gsap";
export { ScrollTrigger, useGSAP };
```

- [ ] **Step 2: Create Lenis Provider**
Create `components/providers/LenisProvider.tsx` with GSAP ticker sync for perfectly timed "scrub" animations.

```tsx
"use client";

import { ReactLenis } from "lenis/react";
import { useEffect, useRef } from "react";
import { gsap } from "@/lib/gsap";

export default function LenisProvider({ children }: { children: React.ReactNode }) {
  const lenisRef = useRef<any>(null);

  useEffect(() => {
    function update(time: number) {
      lenisRef.current?.lenis?.raf(time * 1000);
    }
    gsap.ticker.add(update);
    return () => gsap.ticker.remove(update);
  }, []);

  return (
    <ReactLenis root ref={lenisRef} options={{ autoRaf: false, duration: 1.2 }}>
      {children}
    </ReactLenis>
  );
}
```

- [ ] **Step 3: Wrap Layout**
Update `app/layout.tsx` to include the `LenisProvider`.

- [ ] **Step 4: Commit**
`git add . && git commit -m "feat(motion): setup gsap registry and lenis smooth scroll"`
  </action>
  <verify>
    <automated>grep -r "ReactLenis" app/layout.tsx</automated>
  </verify>
  <done>Lenis smooth scrolling is active and synchronized with GSAP ticker.</done>
</task>

<task type="auto">
  <name>Task 3: Sticky Pixel Navbar & Global Layout</name>
  <files>components/ui/PixelNavbar.tsx, app/layout.tsx</files>
  <action>
- [ ] **Step 1: Create Sticky Pixel Navbar**
Create `components/ui/PixelNavbar.tsx` using Tailwind v4 spacing.

```tsx
export default function PixelNavbar() {
  return (
    <nav className="fixed top-0 left-0 w-full z-50 bg-deep-sky border-b-4 border-midnight-blue p-16 flex justify-between items-center">
      <div className="font-pixel text-midnight-blue text-20">BALANGAY</div>
      <div className="flex gap-24 font-pixel text-14">
        <a href="#hero" className="hover:text-retro-gold transition-colors">HOME</a>
        <a href="#lore" className="hover:text-retro-gold transition-colors">LORE</a>
        <a href="#news" className="hover:text-retro-gold transition-colors">NEWS</a>
      </div>
    </nav>
  );
}
```

- [ ] **Step 2: Apply Global Layout CRT Overlay**
Update `app/layout.tsx` to include the `PixelNavbar` and the `crt-overlay`.

```tsx
import "./globals.css";
import LenisProvider from "@/components/providers/LenisProvider";
import PixelNavbar from "@/components/ui/PixelNavbar";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="antialiased">
        <LenisProvider>
          <div className="crt-overlay" />
          <PixelNavbar />
          <main>{children}</main>
        </LenisProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Commit**
`git add . && git commit -m "feat(layout): add pixel navbar and crt overlay"`
  </action>
  <verify>
    <automated>grep -r "crt-overlay" app/layout.tsx</automated>
  </verify>
  <done>Global layout established with navigation and thematic scanline effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Browser | Untrusted rendering environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Metadata | mitigate | Ensure Next.js metadata is properly configured to avoid leaking internal paths |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for GSAP/Lenis packages |
</threat_model>

<verification>
- Start dev server: `npm run dev`
- Verify smooth scrolling by scrolling the page.
- Verify pixel crispness by inspecting images/text.
- Verify CRT overlay is visible but doesn't block interactions.
</verification>

<success_criteria>
- Next.js 15 app running with Tailwind v4.
- Lenis smooth scrolling functional.
- Pixel-perfect theme applied globally.
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-01-SUMMARY.md` when done
</output>
