---
phase: 01-the-wow-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["components/landing/AncientScrollContainer.tsx", "components/landing/LogbookEntry.tsx", "app/page.tsx"]
autonomous: true
requirements: [CORE-03, CORE-01]
must_haves:
  truths:
    - "Lore sections are contained within thematic scroll-edged containers (D-04)"
    - "Logbook entries animate into view when scrolled (SC-03)"
    - "Typography follows the Serif-for-Lore readability rule (D-08)"
  artifacts:
    - path: "components/landing/AncientScrollContainer.tsx"
      provides: "SVG-masked thematic container"
    - path: "components/landing/LogbookEntry.tsx"
      provides: "Animated lore section component"
---

<objective>
Implement the thematic narrative sections using "Ancient Scroll" containers and animated logbook entries.

Purpose: Deliver the lore and "In-Universe" feeling while maintaining high readability.
Output: Scroll-triggered lore sections with thematic styling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-the-wow-landing-page/01-02-SUMMARY.md
@.planning/phases/01-the-wow-landing-page/01-CONTEXT.md
@.planning/phases/01-the-wow-landing-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AncientScrollContainer</name>
  <files>components/landing/AncientScrollContainer.tsx</files>
  <behavior>
    - `AncientScrollContainer` must apply an SVG mask to create ragged "scroll" edges.
    - Background must be "Sky Mist" (`#F0F9FF`) as per UI-SPEC.
    - Container must support children content.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/scroll-container.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('AncientScrollContainer uses SVG mask and thematic background', () => {
      const component = fs.readFileSync('components/landing/AncientScrollContainer.tsx', 'utf-8');
      expect(component).toContain('mask-image');
      expect(component).toContain('bg-[#F0F9FF]');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/scroll-container.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/AncientScrollContainer.tsx`:
       - Use a div with `mask-image` (inline SVG or CSS variable).
       - Apply `bg-[#F0F9FF]` and `py-16` (64px) padding.
       - Use `max-w-7xl mx-auto` for content centering.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/scroll-container.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-03): implement AncientScrollContainer with SVG mask (D-04)"`
  </action>
  <verify>
    <automated>npx vitest tests/scroll-container.test.ts</automated>
  </verify>
  <done>Scroll container with thematic edges is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LogbookEntry Component</name>
  <files>components/landing/LogbookEntry.tsx, app/page.tsx</files>
  <behavior>
    - `LogbookEntry` must have a two-column layout (Sprite image and Lore text).
    - Lore text must use the `Crimson Text` serif font.
    - Component must animate into view using `ScrollTrigger` (fade-in + slide-up).
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/logbook-entry.test.ts`:
    ```typescript
    import { expect, test } from 'vitest';
    import fs from 'fs';

    test('LogbookEntry uses serif font and has entrance animation', () => {
      const component = fs.readFileSync('components/landing/LogbookEntry.tsx', 'utf-8');
      expect(component).toContain('font-serif');
      expect(component).toContain('opacity-0');
      expect(component).toContain('ScrollTrigger');
    });
    ```

    **Step 2: Run and verify failure**
    Run: `npx vitest tests/logbook-entry.test.ts`
    Expected: FAIL (File not found)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/LogbookEntry.tsx`:
       - Use `useGSAP` to define an entrance animation (`opacity: 0, y: 30` -> `opacity: 1, y: 0`).
       - Apply `font-serif` (Crimson Text) to lore text.
       - Use `flex-col md:flex-row` for responsive sprite/text layout.
    2. Add several `LogbookEntry` instances inside `AncientScrollContainer` in `app/page.tsx`.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/logbook-entry.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(01-03): implement animated LogbookEntry (D-08, SC-03)"`
  </action>
  <verify>
    <automated>npx vitest tests/logbook-entry.test.ts</automated>
  </verify>
  <done>Animated lore entries are integrated into the landing page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS Rendering -> Layout | SVG masking depends on browser CSS support |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Lore text | accept | Lore is public content; no sensitive data exposed |
</threat_model>

<verification>
Verify scroll edges rendering and logbook entrance animations.
</verification>

<success_criteria>
- AncientScrollContainer displays rags-edges via SVG mask.
- Logbook entries stagger into view upon scroll.
- Lore text is readable and uses the specified serif font.
</success_criteria>

<output>
Create `.planning/phases/01-the-wow-landing-page/01-03-SUMMARY.md` when done
</output>
