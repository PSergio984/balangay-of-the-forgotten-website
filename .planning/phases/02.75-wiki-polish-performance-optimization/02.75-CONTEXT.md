# Phase 2.75: Wiki Polish, Performance & SEO Optimization - Context

**Gathered:** 2026-06-07 (Updated)
**Status:** Ready for planning

<domain>
## Phase Boundary

This phase addresses critical issues identified after the initial wiki deployment, specifically regarding image rendering in production, page loading speed, sticky footer behavior, wiki article rendering/formatting, and Search Engine Optimization (SEO) including dynamic meta tags, OpenGraph sharing attributes, and dynamic XML sitemap generation.

</domain>

<decisions>
## Implementation Decisions

### Image Asset Storage
- **D-01:** Configure the Payload `Media` collection's `staticDir` to save directly to `public/media/` and update the seeder to put them there.
- **D-02:** Commit the seeded media assets under `public/media/` to Git. This guarantees the images are bundled directly within Next.js's public build and are immediately available on serverless/production deployments.

### Page Loading Performance
- **D-03:** Implement Next.js `generateStaticParams` (Static Site Generation) on dynamic routes `app/(site)/wiki/[category]/[slug]/page.tsx` and `app/(site)/wiki/[category]/page.tsx` to pre-generate these pages at build time.
- **D-04:** Set up Incremental Static Regeneration (ISR) with a revalidation limit (e.g. `revalidate = 3600`) to fetch fresh records if changes occur in CMS database.

### Sticky Footer shared layout
- **D-05:** Extract the footer from `app/(site)/page.tsx` and turn it into a shared `Footer` component.
- **D-06:** Place the `Footer` component inside the shared `app/(site)/layout.tsx`. Use a CSS flexbox layout (`flex flex-col min-h-screen` on the layout wrapper, `flex-grow` on the main page wrapper, and position the footer at the bottom) so it sticks cleanly on short pages.

### Wiki Layout & Formatting
- **D-07:** Replace the placeholder string (`Detailed lore description from archives...`) on the wiki entry page with a dedicated Lexical rich-text renderer that reads and renders the real JSON structure of `doc.description` into HTML elements.

### SEO & Meta Tags
- **D-08:** Dynamically generate titles and descriptions for dynamic wiki pages by querying Payload CMS records via Next.js `generateMetadata` function.
- **D-09:** Use character/boss/relic artwork dynamically from Payload CMS as the OpenGraph/Twitter sharing image source.

### Sitemap Generation
- **D-10:** Dynamically generate the XML sitemap using Next.js `app/sitemap.ts` by fetching categories and dynamic articles from the Payload CMS API to ensure auto-indexing of new pages.

### the agent's Discretion
- The exact layout style/spacing of the rich text parser (paragraphs, lists, and headers formatting) is at the agent's discretion, as long as it matches the pixel-art/scroll aesthetic.
- The exact sitemap update frequency and dynamic caching strategy is at the agent's discretion.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Navigation & Layouts
- `app/(site)/layout.tsx` — Root layout for the website frontend.
- `app/(site)/page.tsx` — Main landing page where footer is currently defined.

### Wiki Router & Views
- `app/(site)/wiki/page.tsx` — Wiki Hub page.
- `app/(site)/wiki/[category]/page.tsx` — Category listing page.
- `app/(site)/wiki/[category]/[slug]/page.tsx` — Dynamic wiki entry page.

### Sitemap
- `app/sitemap.ts` — Dynamic XML sitemap generator route.

### Payload Collections & Config
- `payload.config.ts` — Main Payload CMS configuration.
- `collections/Media.ts` — Uploads configuration schema for media assets.

### Data Scripts
- `scripts/seed-wiki-full.ts` — Core script used to seed characters, bosses, and relics.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `AncientScrollContainer` — Wrapper for consistent parchment/ancient scroll presentation.
- `PixelNavbar` — Top header bar navigation component.

### Established Patterns
- Route Groups — Using `(site)` for public-facing Next.js pages and `(payload)` to isolate admin paths.

### Integration Points
- `app/(site)/layout.tsx` — Hooking in the shared footer.
- `app/(site)/wiki/[category]/[slug]/page.tsx` — Embedding the Lexical parser and dynamic `generateMetadata`.

</code_context>

<specifics>
## Specific Ideas

- The user wants images to render correctly in the deployment phase.
- The user wants page loads to be fast and responsive, resolving the long load times.
- The footer should stick to the bottom of the viewport on pages with little content.
- Dynamic SEO tags should populate correct wiki titles/descriptions/images dynamically.
- Dynamic XML sitemap must automatically list dynamic category and article routes.

</specifics>

<deferred>
## Deferred Ideas

- None — discussion stayed within phase scope.

</deferred>

---

*Phase: 2.75-Wiki Polish, Performance & SEO Optimization*
*Context gathered: 2026-06-07*
