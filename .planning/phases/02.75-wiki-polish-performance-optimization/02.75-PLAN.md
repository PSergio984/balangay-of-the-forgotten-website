---
phase: 02.75-wiki-polish-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["collections/Media.ts", "app/(site)/wiki/[category]/page.tsx", "app/(site)/wiki/[category]/[slug]/page.tsx", "app/(site)/page.tsx", "app/(site)/layout.tsx", "components/landing/Footer.tsx", "app/sitemap.ts"]
autonomous: true
requirements: [WIKI-01, WIKI-03, CORE-04, SEO-01]
must_haves:
  truths:
    - "Media uploads store directly to public/media and serve from /media (D-01, D-02)"
    - "Dynamic wiki routes category and slug use generateStaticParams and revalidate (D-03, D-04)"
    - "Footer is a shared component sticking to page bottom via flexbox layout (D-05, D-06)"
    - "Wiki page renders real database descriptions with Lexical rich text (D-07)"
    - "Dynamic SEO metadata is generated by querying Payload CMS dynamically (D-08, D-09)"
    - "XML dynamic sitemap generated at app/sitemap.ts (D-10)"
  artifacts:
    - path: "components/landing/Footer.tsx"
      provides: "Extracted shared footer component"
    - path: "app/sitemap.ts"
      provides: "Dynamic XML sitemap route"
---

<objective>
Polish the wiki deployment by moving media uploads to public directory, optimizing performance via SSG/ISR, implementing a shared sticky footer layout, rendering database descriptions via a Lexical parser, and adding dynamic SEO meta tags and sitemap.

Purpose: Eliminate broken production images, speed up navigation, ensure layout uniformity, format wiki descriptions, and optimize indexing.
Output: Production-ready wiki pages with fast load times, layout polish, and full SEO coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02.75-wiki-polish-performance-optimization/02.75-CONTEXT.md
@.planning/phases/02.75-wiki-polish-performance-optimization/02.75-RESEARCH.md
@.planning/phases/02.75-wiki-polish-performance-optimization/02.75-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Media Configuration & Upload Storage</name>
  <files>collections/Media.ts</files>
  <behavior>
    - Media uploads are configured to use public/media directory.
    - Media assets serve from /media static path.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Create `tests/polish.test.ts` to assert Media collection configurations.
    
    **Step 2: Run and verify failure**
    Run: `npx vitest tests/polish.test.ts`
    Expected: FAIL (No config found or incorrect dir)

    **Step 3: Minimal Implementation**
    1. Edit `collections/Media.ts` to set upload properties.
    2. Move files from old root `media/` to `public/media/`.
    3. Re-run seeding script to populate media paths.
    4. Commit media folder.

    **Step 4: Run and verify pass**
    Run: `npx vitest tests/polish.test.ts`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(02.75): update media upload config and move files (D-01, D-02)"`
  </action>
  <verify>
    <automated>npx vitest run tests/polish.test.ts -t "Media Configuration"</automated>
  </verify>
  <done>Media collection correctly configured and assets relocated to public directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSG and ISR Performance Tuning</name>
  <files>app/(site)/wiki/[category]/page.tsx, app/(site)/wiki/[category]/[slug]/page.tsx, app/(site)/page.tsx</files>
  <behavior>
    - Dynamic category routes export generateStaticParams and revalidate = 3600.
    - Dynamic entry routes export generateStaticParams and revalidate = 3600.
    - Landing page exports revalidate = 3600.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Add tests to `tests/polish.test.ts` verifying exports for generateStaticParams and revalidate.

    **Step 2: Run and verify failure**
    Run: `npx vitest run tests/polish.test.ts -t "SSG and ISR"`
    Expected: FAIL (missing exports)

    **Step 3: Minimal Implementation**
    1. Update `app/(site)/wiki/[category]/page.tsx` exporting generateStaticParams and revalidate.
    2. Update `app/(site)/wiki/[category]/[slug]/page.tsx` exporting generateStaticParams (querying CMS collections) and revalidate.
    3. Export revalidate on `app/(site)/page.tsx`.

    **Step 4: Run and verify pass**
    Run: `npx vitest run tests/polish.test.ts -t "SSG and ISR"`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "perf(02.75): implement SSG and ISR on wiki routes (D-03, D-04)"`
  </action>
  <verify>
    <automated>npx vitest run tests/polish.test.ts -t "SSG and ISR"</automated>
  </verify>
  <done>Next.js SSG/ISR caching enabled on all dynamic wiki pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared Sticky Footer Layout</name>
  <files>components/landing/Footer.tsx, app/(site)/layout.tsx, app/(site)/page.tsx</files>
  <behavior>
    - Shared Footer component exists.
    - Layout surrounds children with a flex column pushing the footer down.
    - Homepage uses the shared layout footer instead of inline.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Add tests to `tests/polish.test.ts` verifying existence and placement of Footer component.

    **Step 2: Run and verify failure**
    Run: `npx vitest run tests/polish.test.ts -t "Footer"`
    Expected: FAIL (missing Footer component or incorrect layout elements)

    **Step 3: Minimal Implementation**
    1. Create `components/landing/Footer.tsx` and extract footer structure from landing page.
    2. Integrate `Footer` in `app/(site)/layout.tsx` wrapper using flexbox class rules.
    3. Delete old footer structure in `app/(site)/page.tsx`.

    **Step 4: Run and verify pass**
    Run: `npx vitest run tests/polish.test.ts -t "Footer"`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(02.75): extract and integrate shared sticky footer (D-05, D-06)"`
  </action>
  <verify>
    <automated>npx vitest run tests/polish.test.ts -t "Footer"</automated>
  </verify>
  <done>Shared sticky footer layout implemented and active across all site pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Dynamic Rich-Text Lexical Renderer</name>
  <files>app/(site)/wiki/[category]/[slug]/page.tsx</files>
  <behavior>
    - Lexical rich text descriptions are correctly compiled and rendered on entry pages.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Add test verifying rich text description renders dynamically without showing placeholder content.

    **Step 2: Run and verify failure**
    Run: `npx vitest run tests/polish.test.ts -t "Rich Text"`
    Expected: FAIL (placeholder found)

    **Step 3: Minimal Implementation**
    1. Import `RichText` from `@payloadcms/richtext-lexical/react`.
    2. Replace placeholder string with `RichText` component feeding `doc.description`.
    3. Style custom elements with typography/pixel theme parameters.

    **Step 4: Run and verify pass**
    Run: `npx vitest run tests/polish.test.ts -t "Rich Text"`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(02.75): implement dynamic lexical rich text rendering (D-07)"`
  </action>
  <verify>
    <automated>npx vitest run tests/polish.test.ts -t "Rich Text"</automated>
  </verify>
  <done>Dynamic rich text rendering active on character/boss details pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Dynamic SEO Metadata & Sitemap Route</name>
  <files>app/(site)/wiki/[category]/[slug]/page.tsx, app/sitemap.ts</files>
  <behavior>
    - Dynamic entry page exports generateMetadata fetching title, description, and OG image.
    - app/sitemap.ts exists and dynamically serves sitemap list querying Payload CMS.
  </behavior>
  <action>
    **Step 1: Write a failing test**
    Add tests verifying sitemap route structure and dynamic metadata export.

    **Step 2: Run and verify failure**
    Run: `npx vitest run tests/polish.test.ts -t "SEO and Sitemap"`
    Expected: FAIL (missing sitemap or generateMetadata export)

    **Step 3: Minimal Implementation**
    1. Implement dynamic `generateMetadata` in `app/(site)/wiki/[category]/[slug]/page.tsx`.
    2. Create `app/sitemap.ts` querying all dynamic routes via Payload CMS collections.

    **Step 4: Run and verify pass**
    Run: `npx vitest run tests/polish.test.ts -t "SEO and Sitemap"`
    Expected: PASS

    **Step 5: Commit**
    Command: `git add . && git commit -m "feat(02.75): implement dynamic SEO metadata and sitemap (D-08, D-09, D-10)"`
  </action>
  <verify>
    <automated>npx vitest run tests/polish.test.ts -t "SEO and Sitemap"</automated>
  </verify>
  <done>Dynamic meta tags and XML sitemap generation active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Site Router | External user requests dynamic sitemap or pages |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.75-01 | Information Disclosure | sitemap.ts | mitigate | Do not leak CMS drafts or admin/private urls inside sitemap.ts |
</threat_model>

<verification>
Verify media paths, SSG dynamic params caching, shared layout footer, rich text compiler, and sitemap/SEO metadata.
</verification>

<success_criteria>
- Images render from /media on dynamic wiki routes.
- Category/entry pages pre-rendered statically.
- Footer sticks to viewport bottom dynamically.
- Database rich text description rendered.
- generateMetadata and sitemap.ts active and correct.
</success_criteria>

<output>
Create `.planning/phases/02.75-wiki-polish-performance-optimization/02.75-SUMMARY.md` when done
</output>
